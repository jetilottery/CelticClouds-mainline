define({
  // Optional config in which to overrid configuration options from the template. Alternatively
  // place options in i18n/default/default/config.json to override at skin level.
  matchAnimAmplitude: 6,
  matchAnimPeriod: 2,
      // Time over which the music will fade out on entering the result screen
  resultMusicFadeOutDuration: 0.5,
  // Time between entering the result screen and the terminator audio starting
  resultTerminatorFadeInDelay: 0,
  // Time over which the terminator audio will fade in
  resultTerminatorFadeInDuration: 0.0,
});
